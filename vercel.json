{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["sin1"],
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter satuwork-gateway vercel-build",
  "crons": [{ "path": "/cron/tick", "schedule": "* * * * *" }]
}
