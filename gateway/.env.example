# Gateway 进程环境。复制成 .env（已在 .gitignore）。
#
# 注意：进程**不会自动读 .env**。要用它得显式带上：
#   node --env-file=.env --import tsx src/index.ts
# 直接 `pnpm dev` 的话请用 export 或写在命令前面。
#
# 平台级模型密钥只放在 Gateway。不要把这些写到 bot 的磁盘或环境。

# 数据库。必填，没有默认值——起不来会直接告诉你这一条。
# 宿主机端口是 5434（5432 通常已被别的实例占着），容器内互连用 postgres:5432。
GATEWAY_DATABASE_URL=postgres://satuwork:satuwork@127.0.0.1:5434/satuwork
# 多套环境共用一个库时用它隔开；默认 public。
# GATEWAY_PG_SCHEMA=public

# GATEWAY_HOST=127.0.0.1
# GATEWAY_PORT=3080
# GATEWAY_MACHINE_TOKEN=
# GATEWAY_PLATFORM_TOKEN=
# GATEWAY_ACCESS_HOST=satuwork.com
# Gateway 的公网根地址。部署远端席位时使用；Telegram 渠道使用长轮询，不依赖这个地址。
# GATEWAY_PUBLIC_URL=https://app.example.com

# 本地开发用的桌面转发（生产不要配）。`pnpm dev` 会顺带起一个 TCP 转发，把本机的这个
# 端口原样转给席位机器的管家，然后把机器的「桌面直连地址」填成
# http://<Gateway 所在 IP>:<这里的端口>。
#
# 为的是让桌面 iframe 和 Gateway **同站**：管家那张 cookie 在非 https 下只能是
# SameSite=Lax，浏览器不会把它带进跨站子框，noVNC 的资源和 WebSocket 于是全 401——
# 界面上就是一块黑屏。端口不算 site 的一部分，所以同一个 IP 换个端口就够。
# 生产上这件事由「directUrl 和 Gateway 同一个可注册域」解决，用不着它。
#
# 写法 [监听端口:]<管家主机>:<管家端口>，监听端口默认与管家端口相同。
# SATUWORK_DESK_PROXY=192.168.64.6:8443
#
# 再给一对证书，这一路就改成 TLS 终结：自己收 https，解密后把明文转给管家，并补上
# x-forwarded-proto: https——管家看这个头才发 `SameSite=None; Secure` 的 cookie。
# **桌面壳（satu://localhost）只有这一种走得通**：那个源和任何 IP 都跨站，上面那招
# 「同一个 IP 换端口」只救得了浏览器版。证书要浏览器信得过，本地用 mkcert：
#
#   mkcert -install
#   mkcert -cert-file gateway/.certs/desk.pem -key-file gateway/.certs/desk-key.pem 192.168.64.1
#
# 然后把机器的「桌面直连地址」填成 https://192.168.64.1:8443。
# SATUWORK_DESK_PROXY_CERT=.certs/desk.pem
# SATUWORK_DESK_PROXY_KEY=.certs/desk-key.pem

# 渠道参数通常不用改。Telegram 长轮询默认 30 秒；模型整轮允许 20 分钟。
# GATEWAY_CHANNEL_POLL_TIMEOUT_SECONDS=30
# GATEWAY_CHANNEL_TURN_TIMEOUT_MS=1200000

# 发布包（bot 与机器管家）一律由 CI 上传，凭 GATEWAY_PLATFORM_TOKEN：
#   PUT /platform/bot-releases/:version
#   PUT /platform/manager-releases/:version
# Gateway 自己不打包——包里带 esbuild 的原生二进制，必须在和席位机器同架构的
# Linux 上打，从浏览器或开发机打出来的包装到机器上起不来。
# 上传大小上限，默认 256 MiB。
# GATEWAY_RELEASE_MAX_BYTES=268435456

# 系统管理员。**留空即可**：一个 owner 都没有时，打开首页就是「创建系统管理员」。
# 这两个只给无人值守的自动化用（容器编排、e2e）。
# GATEWAY_OWNER_EMAIL=owner@satuwork.test
# GATEWAY_OWNER_PASSWORD=

# DEEPSEEK_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# docker compose 用（宿主机映射端口、库名口令）
# POSTGRES_PORT=5434
# POSTGRES_USER=satuwork
# POSTGRES_PASSWORD=satuwork
# POSTGRES_DB=satuwork
